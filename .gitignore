# dependencies
node_modules/
.pnpm-store/

# build output
dist/
dist-node/
build/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
.env.*

# secrets / keys (defense in depth — explicit allowlist for tracked test fixtures)
*.pem
*.key
*.p12
*.pfx
!apps/api/test/fixtures/test-private-key-pkcs8.pem
!docker/gotrue-jwt/private.jwk.json

# Local Claude Code config (may contain personal API keys / cached creds)
.claude/settings.local.json

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# test coverage
coverage/

# Railway
.railway/

# Playwright MCP snapshot cache
.playwright-mcp/

# Playwright test artifacts (regenerated every run)
**/playwright-report/
**/test-results/

# Claude Code runtime state
.claude/scheduled_tasks.lock

# Personal workspace — drafts, scratch, non-code notes. Never tracked.
tmp/

# git worktrees
.worktrees/
